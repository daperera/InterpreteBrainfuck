package Interprete;

import java.util.Vector;

public class Code {
	Vector<Operation> code;
	
	public void compile(String instruction) {
		int n = instruction.length();
		for(int i=0; i<n; i++) {
			switch(instruction.charAt(i)) {

			}
		}
	}
}
